{
  "private": true,
  "workspaces": [
    "gatsby-theme-novela",
    "site"
  ],
  "scripts": {
    "build:theme": "yarn --cwd ./gatsby-theme-novela build",
    "build:site": "yarn --cwd ./site build",
    "serve": "yarn --cwd ./site serve",
    "build": "yarn build:theme && yarn build:site"
  }
}
